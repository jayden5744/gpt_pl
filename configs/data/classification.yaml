project_name: "gpt_scratch"
folder_name: "naver_movie_corpus" 
task: "classification" # choice of [pretrain, classification]

language: "ko"

train_path: "data/${data.folder_name}/train.txt"
valid_path: "data/${data.folder_name}/valid.txt"

tokenizer_type: "bpe" # coice of [unigram, bpe, char, word]

dictionary_path: "dictionary/${data.folder_name}"
pretrain_path: ""
model_path: "SavedModel/${data.project_name}/${data.folder_name}"

vocab_size: 8000 # 30000 # 논문에서는 4만